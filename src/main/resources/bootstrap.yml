server:
  port: 8084
management:
  endpoints:
    web:
      exposure:
        include: "*"  
spring:
  application:
    name: department-service
  profiles: local
  cloud:
    config:
      uri: http://localhost:8888